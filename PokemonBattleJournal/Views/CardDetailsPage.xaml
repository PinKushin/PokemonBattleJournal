<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="PokemonBattleJournal.Views.CardDetailsPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:model="clr-namespace:PokemonBattleJournal.Models"
    xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    xmlns:viewmodel="clr-namespace:PokemonBattleJournal.ViewModels"
    x:Name="BasePage"
    Title="{Binding Name}"
    x:DataType="viewmodel:CardDetailsPageViewModel">
    <ContentPage.Behaviors>
        <toolkit:EventToCommandBehavior
            BindingContext="{Binding Path=BindingContext, Source={x:Reference BasePage}, x:DataType={x:Type ContentPage}}"
            Command="{Binding AppearingCommand}"
            EventName="Appearing" />
    </ContentPage.Behaviors>
    <ScrollView>
        <VerticalStackLayout MaximumWidthRequest="300">

            <Image Source="{Binding CardBrief.Image}" />
            <Label Text="{Binding Card.Set.Name}" />
            <Label Text="{Binding Card.Id}" />
            <Label Text="{Binding Card.Attacks[0].Name}" />
            <Label Text="{Binding Attack.Damage}" />
            <Label Text="{Binding Card.Attacks[1].Name}" />
            <Label Text="{Binding Card.Attacks[1].Damage.Integer}" />
            <Button
                Command="{Binding GoBackCommand}"
                Text="Go Back" />

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>